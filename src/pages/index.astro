---
import type { IFrontmatter } from 'astro-boilerplate-components';

import { CTA } from '@/partials/CTA';
import { Hero } from '@/partials/Hero';
import { ProjectList } from '@/partials/ProjectList';
// import { Sponsors } from '@/partials/Sponsors';
import { RecentProducts } from '@/partials/RecentProducts';
import Base from '@/templates/Base.astro';
import { AppConfig } from '@/utils/AppConfig';
import { sortByDate } from '@/utils/Posts';

const allPosts = await Astro.glob<IFrontmatter>('./products/*.md');
const sortedPosts = sortByDate(allPosts);
const productsNumber = sortedPosts.slice(0, 6);

const { title } = AppConfig;
const { description } = AppConfig;
---

<Base head={{ title, description }}>
  <Hero />
  <!-- <Sponsors /> -->
  <RecentProducts postList={productsNumber} />
  <ProjectList />
  <CTA />
</Base>
